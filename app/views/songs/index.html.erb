<h1 class="ui center aligned icon header">Billboard Top 100</h1>
<%# <%= link_to "Add Song +", new_song_path%> 
<br/>
<h2> <%=@songs.length <= 0 ? "No Songs" : ""%> </h2>

<div class="ui center aligned icon header">
<ul>
  <% @songs.each do |song|%>
    <li class="ui list">
    <%= link_to song.name, artist_path(song)%>
     </li>
  <%end%>
</ul>
</div>


